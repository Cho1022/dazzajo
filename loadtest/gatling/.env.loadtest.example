# Dedicated load-test database only. Never point this at production.
LOADTEST_DATABASE_URL=postgresql://buildgraph:CHANGE_ME@rds-endpoint:5432/buildgraph
LOADTEST_CONFIRM_NON_PROD=YES

# Used to create bcrypt hashes in PostgreSQL and to call the real login API.
# Keep the real value in the shell environment, never in this file.
LOADTEST_ACCOUNT_PASSWORD=CHANGE_ME

LOADTEST_HTTP_BASE_URL=http://172.31.10.173:8080
LOADTEST_WS_URL=ws://172.31.10.173:8080/ws/support-chat
LOADTEST_RESULTS_DIR=../results
